# Extra Python dependencies for running the test suite in tests/.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# (CI installs both requirements.txt and this file; this file does not
# duplicate the runtime deps already listed in requirements.txt.)
pytest>=8.0
pytest-bdd>=7.0
pytest-cov>=7.1.0
yamllint>=1.38.0
codespell>=2.4.3
